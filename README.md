# startup

I learned about merging different changes.

/n new line

another new line

div = division
point to another dns record use CNAME. CNAME is an alias, like a pointer to a record.

proper syntax load fonts with css: @import url('url');
proper json syntax {"x":3}

valid uses of javascript: <script></script> , <script src=''/> , <div onclick=''/>

valid javascript functions: function f(x){} , const f = function(x){}, const f = (x) => {}

pals before marriage
content - padding - border - margins

document.querySelector('p').addEventListener , adds eventlistener to a single p element.

src for images. hyperlink is <a href''>

let e = a.map(number => {
    return ('a' + number)
}); takes elements in array a and maps each one to new array with the new values

DOM textContent sets child text for an element

dns subdomain is the whole thing c260.cs.byu.edu

javascript objects valid: {n:1} invalid: {"n"="1"}, {n=1}, {"n"=1}

make script deployable: chmod +x deploy.sh